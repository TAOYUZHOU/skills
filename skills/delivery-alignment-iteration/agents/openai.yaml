interface:
  display_name: "Delivery Alignment Iteration"
  short_description: "Align delivery with finite, tiered evidence gates"
  default_prompt: "Use $delivery-alignment-iteration to freeze a finite threat model, select risk-tiered gates, bind evidence with an external verifier, and close or checkpoint the iteration without an open-ended review loop."
