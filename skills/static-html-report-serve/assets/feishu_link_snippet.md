# Feishu link template

Replace `{PUBLIC_BASE}` with output from `serve_html_report.sh` (e.g. `https://u807812-....seetacloud.com:8443`).

---

**Retro-Engine × ReactSeq 工作汇报（在线 Slides，含动图）**

{PUBLIC_BASE}/retro_engine_work_report_slides.html

说明：需 AutoDL 实例开机；实例释放后链接会变更。

---

Optional index page: `{PUBLIC_BASE}/`
